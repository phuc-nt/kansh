# --- secrets & credentials (never commit) ---
.env
.env.*
*.pem
*.key
*.p12
*credentials*
*secret*
*.token
.auth-session.json
cookies.json
.mcp.json

# --- MK / CK dev toolkit & its artifacts (local-only, never publish) ---
.claude/
.mk/
.ck/
.mk.json
.ck.json
.mkignore
.ckignore
CLAUDE.md
AGENTS.md
plans/
docs/journals/

# --- build & deps ---
node_modules/
dist/

# --- test/tooling residue ---
.smoke-fixture-projects/
.browser-session.json

# --- OS / editor ---
.DS_Store
*.log
